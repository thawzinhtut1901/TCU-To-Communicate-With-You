import { ProfileSetup } from "@/pages/AuthPages"

const ProfileSetupLayout = () => {
  return (
    <div className="flex justify-center bg-custom-gradient w-full h-screen overflow-hidden">
        <ProfileSetup/>
    </div>
  )
}

export default ProfileSetupLayout